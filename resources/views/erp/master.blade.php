@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h3 class="mb-1">{{ $config['title'] }}</h3><div class="text-secondary">Master data</div></div></div>
<div class="card shadow-sm mb-4"><div class="card-header fw-semibold">Tambah {{ $config['title'] }}</div><div class="card-body"><form method="POST" action="{{ route('master.store.'.$type) }}" class="row g-3">@csrf @foreach($config['fields'] as $key=>$field)<div class="col-md-4"><label class="form-label">{{ $field['label'] }}</label>@if($field['type']==='textarea')<textarea name="{{ $key }}" class="form-control" rows="2">{{ old($key) }}</textarea>@elseif($field['type']==='select')<select name="{{ $key }}" class="form-select">@foreach($field['options'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select>@else<input name="{{ $key }}" type="{{ $field['type'] }}" step="{{ $field['step'] ?? 'any' }}" class="form-control" value="{{ old($key) }}" @if($field['required']??false) required @endif>@endif</div>@endforeach<div class="col-12"><button class="btn btn-primary">Simpan</button></div></form></div></div>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr>@foreach($config['columns'] as $column)<th>{{ ucwords(str_replace('_',' ',$column)) }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($config['columns'] as $column)<td>{{ $row->$column }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($config['columns']) }}" class="text-center text-secondary py-4">Belum ada data.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $rows->links() }}</div></div>
@endsection
